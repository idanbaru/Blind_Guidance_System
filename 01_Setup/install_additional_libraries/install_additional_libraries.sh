#!/bin/bash

# Install requests library to handle requests sent to & received from Groq API
sudo -H pip3 install requests

# Install threading library to easily control the worker threads in the system
sudo -H pip3 install thread6


